{
  "name": "devmeeting-rx-in-angular",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prettier": "prettier --write 'bin/*.js'",
    "build:index": "node ./bin/build-index.js",
    "build:tasks": "node ./bin/build-tasks.js",
    "deploy": "git checkout gh-pages; git pull --no-edit origin master; git push origin gh-pages --force; git checkout -"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kapke/devmeeting-rx-in-angular.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/kapke/devmeeting-rx-in-angular/issues"
  },
  "homepage": "https://github.com/kapke/devmeeting-rx-in-angular#readme",
  "dependencies": {
    "pug": "^2.0.0-rc.4"
  },
  "devDependencies": {
    "inquirer": "^3.3.0",
    "prettier": "^1.7.4",
    "ramda": "^0.25.0"
  }
}
